{{ include "dot_claude/CLAUDE.md" -}}
{{/* Shared preferences come from CLAUDE.md above (single source of truth).
     Add Codex-only instructions below this line, e.g. a "## Codex-specific"
     section — they render into ~/.codex/AGENTS.md alongside the shared set. */ -}}
