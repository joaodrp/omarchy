# Global gitignore

# Editor temp files
*.swp
*.swo
*~

# Environment / local overrides
# Convention: <name>.local / <name>.local.<ext> are user-specific
# overrides not meant to be committed (e.g. .env.local).
.env
*.local
*.local.*

# Local-only project directories
_local/
