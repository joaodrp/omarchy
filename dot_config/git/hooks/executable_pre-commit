#!/usr/bin/env bash
# Scan staged content for secrets with gitleaks.
# No-op if gitleaks isn't installed.

if ! command -v gitleaks &> /dev/null; then
    echo "gitleaks not installed, skipping secret scan"
    exit 0
fi

gitleaks protect --staged --verbose
