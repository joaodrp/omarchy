{{ include "dot_claude/CLAUDE.md" -}}
{{/* Shared preferences come from CLAUDE.md above (single source of truth).
     This file replaces OpenCode's native ~/.claude/CLAUDE.md read, so the
     include keeps the shared set. Add OpenCode-only instructions below this
     line, e.g. a "## OpenCode-specific" section. */ -}}
