{{ template "agent-preferences.md" . -}}
{{/* Shared preferences come from .chezmoitemplates/agent-preferences.md above.
     This file replaces OpenCode's native ~/.claude/CLAUDE.md read, so the
     template keeps the shared set. Add OpenCode-only instructions below this
     line, e.g. a "## OpenCode" section. */ -}}
