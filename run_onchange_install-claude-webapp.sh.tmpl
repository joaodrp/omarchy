#!/bin/bash
# Install Claude (claude.ai) as a webapp. Idempotent: the .desktop guard skips reinstall.
set -e
{{ template "install-webapp.sh" (dict
    "name" "Claude"
    "url" "https://claude.ai"
    "icon" "claude-ai.png") }}
