#!/bin/bash
# Load ~/.config/agent-secrets.env into the environment, then exec the command,
# keeping the keys scoped to the wrapped MCP server rather than the session.
set -e
f="$HOME/.config/agent-secrets.env"
[ -r "$f" ] && { set -a; . "$f"; set +a; }
exec "$@"
